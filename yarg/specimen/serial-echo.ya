import("uart");

const device = 1;

// connect gpio before init to avoid spurious 0 byte appearing.
gpio_set_function(8, gpio_func_uart); // TX
gpio_set_function(9, gpio_func_uart); // RX

uart_init(device, 115200, false);

while (true) {
    var input = uart_get_byte(device);
    print input;
    uart_put_byte(device, input);
}
