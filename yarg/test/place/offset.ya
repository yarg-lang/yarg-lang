place struct { 
    muint32@10 ctrl;
    muint32@20 flags;
    } 0xc0000000 more_registers;
peek(more_registers.ctrl);     // expect: peek(c000000a)
peek(more_registers.flags);    // expect: peek(c0000014)

place struct { 
    muint32@0d0 << 0d12 rw;
    muint32@0d1 << 0d12 xor;
    muint32@0d2 << 0d12 set;
    muint32@0d3 << 0d12 clr;
    } 0xc0000000 fourkregister;
peek(fourkregister.rw);     // expect: peek(c0000000)
peek(fourkregister.xor);    // expect: peek(c0001000)
peek(fourkregister.set);    // expect: peek(c0002000)
peek(fourkregister.clr);    // expect: peek(c0003000)

place struct { 
    muint32 begin;
    muint32@0d1 << 0d4 ctrl;
    muint32 ctrl2;
    muint32[2]@0d2 << 0d4 flags;
    muint32 flags2;
    } 0xc0000000 registers2;
peek(registers2.ctrl);     // expect: peek(c0000010)
peek(registers2.flags);    // expect: peek(c0000020)
peek(registers2.begin);     // expect: peek(c0000000)
peek(registers2.ctrl2);    // expect: peek(c0000014)
peek(registers2.flags2);     // expect: peek(c0000028)
