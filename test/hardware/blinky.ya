import("gpio");
import("timer");

gpio_init(pico_led);
gpio_set_direction(pico_led, GPIO_OUT);

var n = 6;

while (n > 0) {
    gpio_put(pico_led, !gpio_get(pico_led));
    sleep_ms(500);
    n = n - 1;
}
